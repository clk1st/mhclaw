; openclaw 的打包产物（dist/*.js）硬编码了嵌套路径如
; openclaw/node_modules/undici/index.js，默认 pnpm 的虚拟 store 布局下
; 这些嵌套 node_modules 不存在，会导致 gateway 子进程启动时
; ERR_MODULE_NOT_FOUND。用 hoisted linker 回到 npm 风格的扁平布局。
node-linker=hoisted
